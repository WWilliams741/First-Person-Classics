using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CameraControls : MonoBehaviour
{

    /*
     * Plans:
     * - Limit directions that camera can go based on parameters (serialized fields)
     * - Disabliing or enabling the camera controls/user input.
     * - Range of fields/angles based on input parameters (serialized fields)
     * - 
     */

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {

    }
}
