using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Follow_guy : MonoBehaviour
{

    [SerializeField] private Transform MineSweeper_Guy;

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        //Vector3 minesweeper_guy = MineSweeper_Guy.transform.position;
        //Quaternion minesweeper_guy_face = MineSweeper_Guy.transform.rotation;
        //transform.position = new Vector3(minesweeper_guy.x, minesweeper_guy.y + 2f, minesweeper_guy.z + 0.25f);
        //transform.rotation = minesweeper_guy_face;
    }
}
